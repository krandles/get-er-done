<h2>List of Projects</h2>
<ul>
  <% @projects.each do |project| %>
    <li><%= link_to project.name, project %> | <%= project.status ? "Complete" : "Incomplete" %></li>
  <% end %>
</ul>
<br>
<%= button_to "Add Project", new_project_path, method: :get %>
