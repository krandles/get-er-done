<h2>Manage Users</h2>
<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.user_name, user %></li>
  <% end %>
</ul>
<br>
<%= button_to "Add User", new_user_path, method: :get, class: 'btn btn-primary' %>